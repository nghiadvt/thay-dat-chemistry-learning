# Database (Docker internal)
DB_HOST=mysql
DB_PORT=3306
DB_NAME=chem_quiz
DB_USER=app_user
DB_PASSWORD=changeme
MYSQL_ROOT_PASSWORD=rootchangeme

# Redis (Docker internal)
REDIS_HOST=redis
REDIS_PORT=6379

# App
WS_PORT=38581
SESSION_SECRET=changeme
SESSION_LIFETIME=480
APP_KEY=base64:q7O0aLkwuy+VHmt8T+RhSmLS3FHQww6T0GuPMg/H75A=

# Host port mapping (docker-compose publish only)
PHP_HOST_PORT=38480
MYSQL_HOST_PORT=38306
REDIS_HOST_PORT=38637
WS_HOST_PORT=38581

# Public URLs (join link + QR + admin Socket.io client)
# Local PC only:     http://localhost:38480
# Phone same Wi-Fi:  http://<your-LAN-IP>:38480   (e.g. http://192.168.1.9:38480)
# Production:        https://your-domain.tld
APP_URL=http://localhost:38480
# Local split ports: http://localhost:38581 | Production (same reverse-proxy): same origin as APP_URL
WS_PUBLIC_URL=http://localhost:38581
